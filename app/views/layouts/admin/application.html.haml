!!!
%html
  %head
    %title Configuration Manager Custom - Thien
    = stylesheet_link_tag "admin"
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = csrf_meta_tag
  %body
    .topbar
      .fill
        .container
          = link_to "Configuration Manager Custom - Thien", admin_root_path, :class => "brand"
          %ul.nav
            %li.active= link_to "Home", "#"
            %li= link_to "Productcats", admin_productcats_path
            %li= link_to "Widgets", admin_widgets_path
            %li= link_to "Post"


    .container
      .content
        .page-header
          = yield :header
        .row
          - if flash[:alert]
            .alert-message.error
              %p= flash[:alert]
          - if flash[:notice]
            .alert-message.info
              %p= flash[:notice]
          .span10
            = yield
      %footer
        %p Copyright &copy; #{Time.now.year} Rails App - Thien Developper Rails.
        %a{href: 'http://dichvusuanhadandung.com'} Link to my page